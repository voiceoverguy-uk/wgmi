import type { Metadata } from "next";
import { Playfair_Display, Inter } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

const playfair = Playfair_Display({
  subsets: ["latin"],
  variable: "--font-playfair",
  display: "swap",
});

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "WGMI — A Private Portfolio of High-Quality Homes",
  description:
    "William George Management & Investments: a growing collection of thoughtfully maintained family houses across West Yorkshire and South Leicestershire.",
  openGraph: {
    title: "WGMI — A Private Portfolio of High-Quality Homes",
    description:
      "A growing collection of thoughtfully maintained family houses across West Yorkshire and South Leicestershire — designed for long-term living.",
    type: "website",
    locale: "en_GB",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${playfair.variable} ${inter.variable} font-sans antialiased`}
      >
        <Navbar />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  );
}
